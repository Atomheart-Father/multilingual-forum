# 🌍 Render超轻量级部署 - 避免Rust编译问题
# 使用较低版本的依赖，确保有预编译wheel

# Web框架 - 使用较低版本避免Rust依赖
fastapi==0.68.0
uvicorn[standard]==0.15.0

# HTTP客户端
httpx==0.24.1

# 环境变量
python-dotenv==0.19.2

# 数据验证 - 使用pydantic v1避免Rust编译
pydantic==1.10.7

# 基础工具
python-dateutil==2.8.2

# 云端翻译API (可选，需要API密钥时取消注释)
# openai==0.27.8
# azure-ai-translation-text==1.0.0
# deepl==1.12.0

# 注意：已移除所有需要编译的依赖
# 如果需要翻译功能，请在环境变量中配置API密钥 